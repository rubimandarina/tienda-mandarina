<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>La Michi Store</title>
    <link href="https://fonts.googleapis.com/css2?family=Pacifico&display=swap" rel="stylesheet">
    <style>
        body {
            background: linear-gradient(to bottom right, #ffffff, #e0e0e0);
            color: #333333;
            font-family: 'Pacifico', cursive; /* Fuente Pacifico */
            margin: 0;
            padding: 0;
        }
        header {
            padding: 20px 20px 5px 20px;
            text-align: left;
            font-size: 3.5em; /* Ajustado para el título más grande */
            font-family: 'Pacifico', cursive; /* Fuente Pacifico */
            color: #212121;
            display: flex; /* Permite alinear el texto y la imagen en la misma línea */
            align-items: center; /* Centra verticalmente los elementos */
        }
        header img.logo-michi { /* Estilos para la nueva imagen del gatito */
            height: 1.8em; /* Ajustado para la imagen más grande */
            width: auto;
            margin-right: 15px; /* ¡AJUSTADO AQUÍ para el espacio a la derecha de la imagen! */
            vertical-align: middle; /* Alinea la imagen con el texto */
        }
        .description {
            padding: 0 20px 20px 20px;
            font-size: 1.2em;
            color: #333333;
        }
        .products {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(200px, 1fr));
            gap: 20px;
            padding: 20px;
        }
        .product {
            background-color: #f5f5f5;
            padding: 15px;
            border-radius: 10px;
            text-align: center;
            box-shadow: 0 0 10px rgba(0,0,0,0.05);
        }
        .product img {
            max-width: 100%;
            height: auto;
            border-radius: 5px;
            margin-bottom: 10px;
        }
        button {
            background-color: #757575;
            color: white;
            border: none;
            padding: 10px 15px;
            border-radius: 5px;
            cursor: pointer;
            margin-top: 10px;
            transition: background-color 0.3s ease;
            font-family: inherit; /* Hereda la fuente principal (Pacifico) */
        }
        button:hover {
            background-color: #5a5a5a;
        }
        .cart {
            position: fixed;
            top: 10px;
            right: 10px;
            background-color: #eeeeee;
            padding: 15px;
            border-radius: 10px;
            color: #212121;
            max-width: 250px;
            box-shadow: 0 0 10px rgba(0,0,0,0.1);
            font-family: 'Pacifico', cursive; /* Fuente Pacifico */
            z-index: 1000;
        }
        #cart-items {
            list-style: none;
            padding-left: 0;
            margin-top: 10px;
            max-height: 200px;
            overflow-y: auto;
            border-bottom: 1px solid #ccc;
            padding-bottom: 10px;
            margin-bottom: 10px;
        }
        #cart-items li {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 5px 0;
        }
        #cart-items li button {
            background-color: #ef5350;
            padding: 3px 7px;
            font-size: 0.8em;
            margin-top: 0;
        }
        #cart-items li button:hover {
            background-color: #d32f2f;
        }
        #cart-total {
            font-weight: bold;
            margin-bottom: 10px;
            text-align: right;
            font-size: 1.1em;
        }
        #checkout {
            margin-top: 10px;
            background-color: #424242;
            width: 100%;
        }
    </style>
</head>
<body>
    <header>
        <img src="https://i.postimg.cc/1zVd6tS0/cat.png" alt="Logo de gatito" class="logo-michi">
        La Michi Store </header>
    <div class="description">
        ola humano, gracias por visitar mi sitio, toma una galleta 🍪
    </div>

    <div class="cart">
        <strong>Carrito de Michi-Compras:</strong>
        <ul id="cart-items">
            </ul>
        <div id="cart-total">Total: $0.00</div>
        <button id="checkout" onclick="checkout()">Finalizar compra</button>
    </div>

    <div class="products">
        <div class="product">
            <h3>Blusa Michi-Fashion</h3>
            <p>Precio: $<span class="price">25.99</span></p>
            <button onclick="addToCart({name: 'Blusa Michi-Fashion', price: 25.99})">Agregar al carrito</button>
        </div>
        <div class="product">
            <h3>Zapatos Michi-Patitas</h3>
            <p>Precio: $<span class="price">49.95</span></p>
            <button onclick="addToCart({name: 'Zapatos Michi-Patitas', price: 49.95})">Agregar al carrito</button>
        </div>
        <div class="product">
            <h3>Collar Michi-Glam</h3>
            <p>Precio: $<span class="price">15.00</span></p>
            <button onclick="addToCart({name: 'Collar Michi-Glam', price: 15.00})">Agregar al carrito</button>
        </div>
        <div class="product">
            <h3>Pulsera Michi-Amistad</h3>
            <p>Precio: $<span class="price">10.50</span></p>
            <button onclick="addToCart({name: 'Pulsera Michi-Amistad', price: 10.50})">Agregar al carrito</button>
        </div>
        <div class="product">
            <h3>Aretes Michi-Brillo</h3>
            <p>Precio: $<span class="price">12.75</span></p>
            <button onclick="addToCart({name: 'Aretes Michi-Brillo', price: 12.75})">Agregar al carrito</button>
        </div>
        <div class="product">
            <h3>Gorra Michi-Cool</h3>
            <p>Precio: $<span class="price">20.00</span></p>
            <button onclick="addToCart({name: 'Gorra Michi-Cool', price: 20.00})">Agregar al carrito</button>
        </div>
    </div>

    <script>
        let cart = []; // Array para almacenar los productos en el carrito

        function updateCartDisplay() {
            const cartList = document.getElementById("cart-items");
            cartList.innerHTML = ""; // Limpiar la lista actual
            let total = 0;

            cart.forEach((item, index) => {
                const li = document.createElement("li");
                li.innerHTML = `
                    <span>${item.name} - $${item.price.toFixed(2)}</span>
                    <button onclick="removeFromCart(${index})">X</button>
                `;
                cartList.appendChild(li);
                total += item.price;
            });

            document.getElementById("cart-total").textContent = `Total: $${total.toFixed(2)}`;
        }

        function addToCart(product) {
            cart.push(product);
            updateCartDisplay();
        }

        function removeFromCart(index) {
            cart.splice(index, 1); // Eliminar el elemento en la posición 'index'
            updateCartDisplay();
        }

        function checkout() {
            if (cart.length === 0) {
                alert("Tu carrito de Michi-Compras está vacío. ¡Agrega algo lindo primero!");
            } else {
                let purchaseSummary = "¡Gracias por tu Michi-Compra! Has adquirido:\n\n";
                cart.forEach(item => {
                    purchaseSummary += `- ${item.name} ($${item.price.toFixed(2)})\n`;
                });
                const total = cart.reduce((sum, item) => sum + item.price, 0);
                purchaseSummary += `\nTotal a pagar: $${total.toFixed(2)}`;

                alert(purchaseSummary);
                cart = []; // Vaciar el carrito después de la compra
                updateCartDisplay();
            }
        }
    </script>
</body>
</html>
